apiVersion: v1
kind: Service
metadata:
  name: k8s-ui
  namespace: kube-system
  labels:
    name: k8s-ui
spec:
  type: NodePort
  ports:
  - port: 9090
    nodePort: 30005
  selector:
    name: k8sui
